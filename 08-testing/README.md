# 08 – Testing Terraform

This module introduces basic testing and validation patterns for Terraform:

- `terraform fmt` for formatting
- `terraform validate` for static validation
- A simple shell script to run checks
- A starter Terratest (Go) file to demonstrate automated testing

> Note: Terratest requires Go tooling installed.
